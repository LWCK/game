import './App.css'

const App = () => {

	// console.log(gameList && gameList.data[1].attributes.game.data[0].attributes.url);
	// console.log(rule && rule.data.attributes.text);

	return (
		<></>
		// <Game gameApi={gameList} ruleApi={rule} />
	)
}


export default App
